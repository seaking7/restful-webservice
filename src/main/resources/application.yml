server:
  port: 8088

logging:
  level:
    org.springframework: DEBUG

spring:
  messages:
    basename: messages
  security:
    user:
      name: taekyung
      password: passw0rd

management:
  endpoints:
    web:
      exposure:
        include: "*"


